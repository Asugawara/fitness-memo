//! カレンダータブ。**Wave 3a（Phase 3a）で実装する。**
//!
//! 今は `views/mod.rs` のモジュール宣言を先に確定させるための stub。
//! Wave 3・4 の 2 ワーカーが `mod.rs` を同時編集する衝突を構造的に消すため、
//! 宣言だけ先に置いてある。
//!
//! 実装時の要件（計画「2. カレンダー」より）:
//! - 月グリッド（日〜土）+ 前後月ナビ。実施日に部位カラーのドット（最大 3 色）
//! - 月フッタに「実施 N 日 / 合計 X kg·回」
//! - **記録がない日をタップしても「記録なし」＋「この日に記録する」を必ず出す**
//! - どちらも今日タブをその日付で開く（`DateCtx::selected` を書き換えてタブ切替）

use leptos::prelude::*;

#[component]
pub fn Calendar() -> impl IntoView {
    view! { <p class="stub">"カレンダー（Wave 3a で実装）"</p> }
}
